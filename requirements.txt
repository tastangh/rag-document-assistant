# Core OCR and layout analysis
paddleocr>=3.5.0,<3.6.0
paddlex[ocr]>=3.5.1,<3.6.0

# Windows'ta pratikte stabil olan paket CPU paddlepaddle'dir.
# Tam Paddle GPU hizlandirma icin Linux/WSL2 + uygun wheel onerilir.
paddlepaddle==3.3.0

# PDF page rendering
pymupdf>=1.24.0

# Image loading and array utilities
opencv-python-headless>=4.10.0
numpy==1.26.4

# HTML table -> Markdown grid conversion
beautifulsoup4>=4.12.0

# Faz 2: chunking + embedding
sentence-transformers>=3.0.0
transformers>=4.44.0,<5.0.0
langchain-text-splitters>=0.2.0

# Torch GPU wheels (CUDA) are served from PyTorch index.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# Faz 3: vector DB + retrieval
chromadb>=1.0.0

# Faz 6: UI
streamlit>=1.35.0

# Faz 6: RAG Triad Evaluation (optional at runtime)
ragas>=0.2.0
